---
import { Card } from "../ui/card";
import { LucideIcon } from "../layouts/lucide-icon";

const active = Astro.url.pathname;

const navItems = [
  {
    href: "/",
    icon: "home",
  },
  {
    href: "/article/",
    icon: "article",
  },
  {
    href: "/work/",
    icon: "work",
  },
  {
    href: "/vlog/",
    icon: "vlog",
  },
  {
    href: "/contact/",
    icon: "contact",
  },
];
---

<nav class="sticky bottom-8 mx-auto text-notion-tag-gray">
  <Card className="border-zink-8 flex space-x-6 rounded-full px-6 py-3">
    {
      navItems.map((item) => (
        <a href={item.href}>
          <LucideIcon type={item.icon} isActive={active === item.href} />
        </a>
      ))
    }
  </Card>
</nav>
